let userNames: string[] = ['Admin','Ali','Dua','Bisma','Hira'];
if(userNames.length === 0){
    console.log('we need to find some users!')
}
else{
    userNames = [];
    console.log(`All user names has been removed.` + userNames.length);
}